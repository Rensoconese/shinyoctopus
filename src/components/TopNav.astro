---
const pathname = Astro.url.pathname.replace(/\/+$/, "") || "/";

const links = [
  { href: "/", label: "Home" },
  { href: "/seo", label: "SEO Audit" },
  { href: "/accesibilidad", label: "Accessibility" },
  { href: "/servers", label: "Servers" },
  { href: "/blog", label: "Blog" },
  { href: "/contact", label: "Contact" },
];

const isActive = (href: string) => {
  if (href === "/") return pathname === "/";
  return pathname === href || pathname.startsWith(`${href}/`);
};
---

<header class="topbar">
  <a href="/" class="topbar-brand" aria-label="Shiny Octopus - Home">
    <svg width="24" height="24" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M10.2555 21.429C9.5817 21.429 8.92342 21.2925 8.2987 21.0222L7.91406 20.8579L12.7543 9.76286C13.534 7.97895 15.3023 6.82422 17.259 6.82422C17.9328 6.82422 18.5911 6.96066 19.2158 7.23103L19.5979 7.39527L14.7576 18.4903C13.978 20.2742 12.2097 21.429 10.2503 21.429H10.2555Z" fill="white"></path>
      <path d="M19.1922 1.42857C17.4633 0.648479 15.637 0.251953 13.7645 0.251953C8.39844 0.251953 3.54023 3.51228 1.38559 8.55567C-1.60269 15.5558 1.51128 23.7532 8.32662 26.8269C10.0555 27.6044 11.8818 28.001 13.7517 28.001C19.1203 28.001 23.9811 24.7407 26.1332 19.6947C29.1215 12.6946 26.0075 4.49711 19.1922 1.42857ZM23.8323 18.6761C22.0753 22.7865 18.12 25.4456 13.7517 25.4456C12.2332 25.4456 10.748 25.1242 9.34238 24.4919C3.78391 21.9831 1.24451 15.2914 3.68644 9.57679C5.4435 5.4664 9.39881 2.80734 13.7645 2.80734C15.2856 2.80734 16.7708 3.12871 18.1764 3.76367C23.7374 6.26981 26.2743 12.9589 23.8323 18.6761Z" fill="white"></path>
    </svg>
    <span class="topbar-name">Shiny Octopus<span class="topbar-tagline">Design & Development Studio</span></span>
  </a>
  <button
    class="topbar-toggle"
    type="button"
    aria-label="Open primary navigation"
    aria-controls="primary-navigation"
    aria-expanded="false"
    data-topbar-toggle
  >
    <span class="topbar-toggle-line"></span>
  </button>
  <nav id="primary-navigation" class="topbar-nav" aria-label="Primary navigation" data-open="false">
    <ul class="topbar-menu">
      {links.map((link) => (
        <li>
          <a class:list={[isActive(link.href) && "active"]} href={link.href} aria-label={link.label}>{link.label}</a>
        </li>
      ))}
    </ul>
  </nav>
</header>

<script is:inline>
  (() => {
    const toggle = document.querySelector("[data-topbar-toggle]");
    const nav = document.getElementById("primary-navigation");
    if (!toggle || !nav) return;

    const setOpen = (isOpen) => {
      toggle.setAttribute("aria-expanded", String(isOpen));
      toggle.setAttribute("aria-label", isOpen ? "Close primary navigation" : "Open primary navigation");
      nav.dataset.open = String(isOpen);
    };

    toggle.addEventListener("click", () => {
      setOpen(toggle.getAttribute("aria-expanded") !== "true");
    });

    nav.querySelectorAll("a").forEach((link) => {
      link.addEventListener("click", () => setOpen(false));
    });

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape") setOpen(false);
    });
  })();
</script>

<style is:inline>
  .topbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 40;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem 1.5rem;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .topbar-brand {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: #fff;
    flex-shrink: 0;
  }

  .topbar-name {
    font-family: var(--font-primary);
    font-weight: 700;
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    line-height: 1.15;
    text-transform: uppercase;
    display: flex;
    flex-direction: column;
  }

  .topbar-tagline {
    font-weight: 500;
    font-size: 0.52rem;
    opacity: 0.6;
    letter-spacing: 0.06em;
  }

  /* Toggle: hidden on desktop */
  .topbar-toggle {
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    background: transparent;
    border: 0;
    color: #fff;
    cursor: pointer;
    display: none;
    -webkit-tap-highlight-color: transparent;
    position: relative;
    z-index: 60;
  }

  .topbar-toggle:focus-visible {
    outline: 2px solid #fff;
    outline-offset: 3px;
    border-radius: 0.25rem;
  }

  .topbar-toggle-line {
    background: #fff;
    display: block;
    height: 2px;
    position: relative;
    width: 18px;
  }

  .topbar-toggle-line::before,
  .topbar-toggle-line::after {
    background: #fff;
    content: '';
    display: block;
    height: 100%;
    position: absolute;
    transition: all 0.2s ease-out;
    width: 100%;
  }

  .topbar-toggle-line::before { top: 5px; }
  .topbar-toggle-line::after { top: -5px; }

  /* X animation */
  .topbar-toggle[aria-expanded="true"] .topbar-toggle-line {
    background: transparent;
  }
  .topbar-toggle[aria-expanded="true"] .topbar-toggle-line::before {
    transform: rotate(-45deg);
    top: 0;
  }
  .topbar-toggle[aria-expanded="true"] .topbar-toggle-line::after {
    transform: rotate(45deg);
    top: 0;
  }

  /* Nav, mobile: collapsed by default */
  .topbar-nav {
    width: 100%;
    position: fixed;
    top: 0; left: 0;
    background: rgba(0, 0, 0, 0.97);
    overflow: hidden;
    max-height: 0;
    visibility: hidden;
    transition: max-height 0.3s ease-out;
    z-index: 50;
    pointer-events: none;
  }

  /* Open state */
  .topbar-nav[data-open="true"] {
    max-height: 100vh;
    visibility: visible;
    pointer-events: auto;
  }

  .topbar-menu {
    list-style: none;
    margin: 0;
    padding: 5rem 0 2rem;
    display: flex;
    flex-direction: column;
  }

  .topbar-menu a {
    display: block;
    padding: 1rem 2rem;
    color: rgba(255, 255, 255, 0.85);
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.9rem;
    font-family: var(--font-primary);
    transition: background 0.2s ease;
  }

  .topbar-menu a:hover,
  .topbar-menu a:focus-visible {
    background: rgba(255, 255, 255, 0.06);
    color: #fff;
    outline: none;
  }

  .topbar-menu a.active {
    background: rgba(255, 255, 255, 0.1);
    color: #fff;
    font-weight: 700;
  }

  /* Desktop: min-width approach, show nav inline, hide hamburger */
  @media (min-width: 48.0625rem) {
    .topbar-toggle {
      display: none;
    }

    .topbar-nav {
      position: static;
      max-height: none;
      width: auto;
      height: auto;
      background: transparent;
      overflow: visible;
      pointer-events: auto;
      visibility: visible;
    }

    .topbar-menu {
      flex-direction: row;
      align-items: center;
      gap: 0.35rem;
      padding: 0.3rem;
      border: 1px solid rgba(255, 255, 255, 0.12);
      border-radius: 999px;
    }

    .topbar-menu a {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.42rem 0.62rem;
      font-size: 0.72rem;
      border-radius: 999px;
      border: 1px solid transparent;
      white-space: nowrap;
    }

    .topbar-menu a:hover,
    .topbar-menu a:focus-visible {
      border-color: rgba(255, 255, 255, 0.35);
      background: transparent;
      color: #fff;
      outline: none;
    }

    .topbar-menu a.active {
      background: #fff;
      color: #000;
      border-color: #fff;
      font-weight: 500;
    }
  }

  /* Mobile: show hamburger */
  @media (max-width: 48rem) {
    .topbar {
      padding: 0.6rem 0.75rem;
    }

    .topbar-toggle {
      display: inline-flex;
    }
  }
</style>
